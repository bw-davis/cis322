Templates holds all of the .html templates needed to render the appropriate pages
-- create_user will let user create a new user
-- login.html will allow the user to login and direct them to dashboard.html
-- dashboard.html will display the username if the user was able to login correctly